{
  "dependencies": {
    "firebase": "^9.1.3",
    "firebase-tools": "^9.21.0"
  },
  "scripts": {
    "test": "echo aaa",
    "restart": "npx kill-port 5000 && npx kill-port 6969 && npx kill-port 8080 && npx kill-port 4400 && firebase emulators:start",
    "kill": "npx kill-port 5000 && npx kill-port 6969 && npx kill-port 8080 && npx kill-port 4400"
  }
}
